package richard.feldtz.draft_pool.service;

public class SchedulerService {
    // TODO: this class needs to run every 5 minutes and check for new final games
    // It also needs to consider the date and call a points assigner service to add the correct number of
    // points to the total for the win
}
